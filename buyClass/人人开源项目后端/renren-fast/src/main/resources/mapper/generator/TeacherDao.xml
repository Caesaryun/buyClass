<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.generator.dao.TeacherDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="io.renren.modules.generator.entity.TeacherEntity" id="teacherMap">
        <result property="id" column="id"/>
        <result property="nickName" column="nick_name"/>
        <result property="photo" column="photo"/>
        <result property="introduction" column="introduction"/>
        <result property="title" column="title"/>
        <result property="curriculums" column="curriculums"/>
        <result property="rating" column="rating"/>
        <result property="students" column="students"/>
    </resultMap>

    <select id="searchCurriculumTeacher" parameterType="Integer" resultType="HashMap">
        SELECT
         t.nick_name,
		t.title,
		t.photo,
		t.curriculums,
		t.students,
		t.rating,
		t.introduction
       FROM tb_curriculum_teacher ct JOIN tb_teacher t
       ON ct.teacher_id=t.id
       WHERE ct.curriculum_id=#{curriculum_id}
    </select>



</mapper>